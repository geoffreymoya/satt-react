import { CampaignListProps } from '@/types/types'
import { useAccount } from '@starknet-react/core'
import { useState } from 'react' // apply params
export default function CampaignResults({campaignlist}:CampaignListProps) {

}